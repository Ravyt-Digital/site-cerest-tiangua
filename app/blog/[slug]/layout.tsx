import type { Metadata } from "next";
const data:Record<string,[string,string]>={silicose:["Silicose: por que a poeira de sílica exige atenção permanente","Entenda os riscos da sílica, os sinais da silicose e as medidas que protegem trabalhadores expostos à poeira."],notificacao:["Acidente de trabalho: quando e como buscar orientação","Saiba quando buscar atendimento e por que a notificação de acidentes protege outros trabalhadores."],visat:["Como a vigilância transforma ambientes de trabalho","Conheça como a VISAT identifica riscos e orienta medidas para ambientes de trabalho mais seguros."]};
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const [title,description]=data[slug]||["Artigo do CEREST Tianguá","Informação sobre saúde e segurança do trabalhador."];return {title,description,openGraph:{title,description,type:"article"},twitter:{card:"summary_large_image",title,description}}}
export default function ArticleLayout({children}:{children:React.ReactNode}){return children}
